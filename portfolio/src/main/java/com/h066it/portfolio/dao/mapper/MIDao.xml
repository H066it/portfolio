<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.h066it.portfolio.dao.IDao">

	<!-- freeBoard #1 nav -->

	<select id="list" resultType="com.h066it.portfolio.dto.Dto" parameterType="com.h066it.portfolio.vo.PageVo">
		SELECT * FROM freeBoard ORDER BY bId DESC
	</select>

	<insert id="write">
		INSERT INTO freeBoard (gId, bId, bWriter, bTitle,
		bContent, bCount) VALUES (1, freeBoard_seq.nextval, #{param1},
		#{param2}, #{param3}, 0)
	</insert>

	<select id="view" resultType="com.h066it.portfolio.dto.Dto">
		SELECT * FROM freeBoard WHERE bId = #{param1}
	</select>

	<delete id="delete">
		DELETE FROM freeBoard WHERE bId = #{param1}
	</delete>
	
	<update id="update">
		UPDATE freeBoard SET bWriter = #{param2}, bTitle = #{param3}, bContent = #{param4} WHERE bId = #{param1}
	</update>
	
	<update id="countUpdate">
		UPDATE freeBoard SET bCount = bCount + 1 WHERE bId = #{param1}
	</update>
	
	<select id="searchList" resultType="com.h066it.portfolio.dto.Dto">
		SELECT * FROM freeBoard WHERE
		<if test="searchType.equals('bTitle')">bTitle LIKE '%'||#{keyword}||'%'</if>
		<if test="searchType.equals('bContent')">bContent LIKE '%'||#{keyword}||'%'</if>
		<if test="searchType.equals('bTitle_bContent')">bTitle LIKE '%'||#{keyword}||'%' OR bContent LIKE '%'||#{keyword}||'%'</if>
		<if test="searchType.equals('bWriter')">bWriter LIKE '%'||#{keyword}||'%'</if>
		ORDER BY bId DESC
	</select>

</mapper>